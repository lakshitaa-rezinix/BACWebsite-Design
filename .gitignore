/.claude
node_modules
dist
revisions/
Re_ Preview of the New Website Design/
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example
server/node_modules
server/uploads
docs/

# Real people's certificate PDFs — must never be served from the web root
private-files/
server/.s3-backup-manifest.json
